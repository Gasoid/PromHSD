apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "promdhsd.fullname" . }}-test-connection"
  labels:
    {{- include "promdhsd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "promdhsd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
